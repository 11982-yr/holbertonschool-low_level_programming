#include <stdio.h>

/**
 * main - check the code.
 *
 * @argc: argument count.
 * @argv: argument values.
 *
 * Return: Always 0.
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
